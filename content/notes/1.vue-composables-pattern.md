---
title: Vue 3 Composable 패턴 정리
description: Vue 3에서 Composable 패턴을 활용한 로직 재사용 방법
date: '2026-04-09'
tags: ['Vue', 'TypeScript', 'Pattern']
---

# Vue 3 Composable 패턴

## Composable이란?
Vue 3의 Composition API를 활용하여 상태와 로직을 캡슐화하고 재사용하는 함수입니다.

## 기본 구조

```typescript
export function useCounter(initialValue = 0) {
  const count = ref(initialValue)

  function increment() { count.value++ }
  function decrement() { count.value-- }
  function reset() { count.value = initialValue }

  return { count, increment, decrement, reset }
}
```

## 설계 원칙

1. **단일 책임**: 하나의 관심사만 다룰 것
2. **반응형 반환**: `ref`나 `computed`를 반환하여 반응성 유지
3. **명확한 인터페이스**: TypeScript로 입출력 타입 정의
4. **부작용 관리**: `onMounted`, `onUnmounted`에서 정리

## 실전 예시

```typescript
export function useNavigation() {
  const route = useRoute()

  const currentBoard = computed(() => {
    const path = route.path.split('/').filter(Boolean)
    return path[0] || ''
  })

  return { currentBoard }
}
```
